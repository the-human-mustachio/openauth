---
title: Introduction
description: A library-only IdP you embed inside your host application.
---

`@_mustachio/openauth` is a **server-side IdP library** for OAuth 2.1 /
OIDC Core. You import it into your host application; you don't deploy it
as a separate service.

The library is the **auth brain**. The host application is the product —
it owns the console UI, the data model (Users, Apps, Workspaces,
App-Tenants), authorization (RBAC), and admin mutations. The library
owns identity endpoints, per-tenant isolation, the auth-method registry,
and the standards posture.

This boundary is not negotiable. There is no admin HTTP surface, no
console, no built-in user model. If those are what you need, you need a
product layer on top of this library — which is the point.

## What you get

- **OAuth 2.1 / OIDC Core endpoints.** `/authorize`, `/token`, `/cb/*`,
  `/userinfo`, `/revoke`, `/introspect`, `/.well-known/*`.
- **Multi-tenant from day one.** `TenantId` is an opaque partition key.
  Every flow, token, audit event, and method-instance cache is keyed by
  it.
- **15 upstream OAuth/OIDC providers**: Google, GitHub, Apple, Microsoft,
  Discord, Facebook, LinkedIn, Slack, Spotify, Twitch, X, Yahoo,
  JumpCloud, Keycloak, Cognito. Plus generic `oauth2Factory` /
  `oidcFactory` for everything else.
- **Built-in credential methods**: password (argon2id), email/SMS code,
  WebAuthn passkey, M2M client-credentials.
- **Production storage adapters**: Postgres, DynamoDB, Cloudflare D1, KV,
  Durable Objects, AWS KMS (key wrapping), in-memory (dev/tests).
- **Runs anywhere a fetch handler runs.** Node, Bun, AWS Lambda,
  Cloudflare Workers.

## What you give up

- No tenant-provisioning UI. Build one against `ConfigStore` /
  `MethodStore` in your host.
- No user model. The library doesn't know what a "user" is — you map
  method results into your subject types via the `success` callback.
- No built-in admin API. The host imports adapters in-process; there is
  no separate boundary to protect.
- No themed React/JSX UI. The library ships minimal server-rendered
  HTML forms (`src/ui/forms.ts`) and a provider picker
  (`src/ui/picker.ts`); render your own from the host if you need more.

## Where to go next

- [**Getting started**](/docs/getting-started/) — the minimum-viable
  embedding (Node + Postgres + password + Google).
- [**Embedding pattern**](/docs/concepts/embedding/) — what the
  host/library boundary looks like and why.
- [**`createIdP` reference**](/docs/reference/idp/) — the full
  `IdPOptions` surface.

## In the repo

These two files are the authoritative deep dive:

- `packages/openauth/ARCHITECTURE.md` — mental model, type layout, tenant
  recovery, flow lifecycle, embedding pattern, phase status.
- `packages/openauth/INTEGRATION.md` — end-to-end embedding guide for
  hosts.

This site mirrors them in a navigable shape, but the source docs are
where to look when something disagrees.
