{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["bun"]
  },
  "//": "Covers the whole test tree, not just test/types. `bun test` strips types without checking them, so anything not listed here drifts silently — which is how CodePayload.authTime and a Result narrowing went stale across several releases.",
  "include": ["src", "test"]
}
